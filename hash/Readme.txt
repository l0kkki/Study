Реализовать хеш-таблицу с h(k) = k mod x, где k - значение ключа, x - любое
число от 5 до 15 (выбрать одно число)
struct Node
{
int k;
Node* next;
char value;
};
Заполнить хеш-таблицу случайными значениями с ключами от 0 до
10000000000 и получать значения из хеш-таблицы по ключу.